The diagnosis history cards render correctly from Firestore, but clicking a diagnosis card either does nothing or opens a blank page.

This is now a frontend routing + detail rendering issue.

Fix all diagnosis detail page issues completely.

Current symptoms:
- History cards load correctly
- Firestore data exists correctly
- Clicking diagnosis card fails
- Detail page becomes blank
- 6 diagnosis detail cards do not render

Implement these fixes:

1. Ensure each diagnosis card is clickable:
onClick={() => navigate(`/diagnosis/${doc.id}`)}

2. Create dynamic route:
<Route path="/diagnosis/:id" element={<DiagnosisDetail />} />

3. In DiagnosisDetail page:
Use useParams() to fetch Firestore document by ID directly.

DO NOT rely only on location.state because refresh/direct navigation breaks it.

4. Fetch Firestore document safely:
- loading state
- error state
- document existence check

5. Add defensive rendering everywhere:
Use optional chaining:
diagnosis?.groqResponse?.diseaseDetection

6. Prevent crashes from undefined arrays:
Example:
diagnosis?.groqResponse?.treatment?.map(...)

7. Firestore structure is nested:
groqResponse:
  diseaseDetection
  treatment
  fertilizerRecommendation
  confidenceScore

Frontend must map nested structure correctly.

8. Render detailed diagnosis UI cards:
- Plant identified
- Disease name
- Severity
- Confidence
- Symptoms
- Affected parts
- Short-term treatment
- Long-term treatment
- Fertilizer recommendation
- Prevention tips

9. Add fallback UI:
If data missing:
"Diagnosis data unavailable"

10. Add loading skeleton while fetching.

11. Add console logs:
- route params
- fetched document
- nested groqResponse structure

12. Fix blank white page completely by wrapping render logic with null checks.

13. Ensure history page navigation works on every card.

14. Add Firestore fetch by document ID:
doc(db, "plant_diagnosis_history", id)

15. Prevent React render crashes from undefined nested fields.

16. Add ErrorBoundary component around diagnosis detail page. 




#final understanding
There are 2 issues to fix in the diagnosis feature:

Issue 1 — Diagnosis results not displaying after analysis:
The Supabase Edge Function is working correctly and 
returning data. Firestore is also saving the data 
correctly. But after the analysis completes, the page 
goes blank instead of showing the 6 result cards.

Fix this by:
- Check the state variable that holds the diagnosis 
  result after the edge function returns
- Make sure the result state is being set correctly 
  after receiving the response from the edge function
- Make sure the 6 result cards component is 
  conditionally rendered when result state is not null
- Add a console.log to print the result data received 
  from edge function so we can confirm data is arriving
- Make sure the page does not navigate away or reset 
  after diagnosis completes

Issue 2 — History cards are not clickable:
On the /history page, the diagnosis history cards 
are showing correctly with image, plant name, disease 
and confidence but clicking on them does nothing.

Fix this by:
- Add an onClick handler to each history card
- When a card is clicked, fetch the full diagnosis 
  data for that record from Firestore using the 
  document ID
- Display the full 6 result cards in a modal or 
  expand in place so user can see complete past 
  diagnosis details including treatment plan, 
  fertilizer recommendations and prevention tips
- The modal should have a close button to go back 
  to history list

Both fixes should not break the existing working 
pipeline of Cloudinary upload, edge function call 
and Firestore saving.