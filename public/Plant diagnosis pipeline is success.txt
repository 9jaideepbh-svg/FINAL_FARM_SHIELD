Plant diagnosis pipeline is successfully completing and storing data in Firestore, but frontend result page becomes blank after diagnosis.

Backend is confirmed working:
- Cloudinary upload works
- PlantNet works
- Plant.id works
- Groq sanitization works
- Firestore storage works

Issue is frontend rendering/navigation.

Fix all frontend rendering issues:

1. Ensure diagnosis result page receives complete diagnosis data after successful API response.

2. After successful diagnosis:
navigate properly with response state OR fetch document by ID from Firestore.

3. Add loading and null safety checks everywhere:
Use optional chaining:
data?.groqResponse?.diseaseDetection

4. Prevent blank page crashes:
Wrap result components with defensive rendering.

5. Add proper error boundaries.

6. Ensure frontend field mapping matches Firestore structure exactly.

Firestore structure includes:
- diseaseName
- groqResponse
- confidenceScore
- diseaseDetection
- treatment
- fertilizerRecommendation
- affectedParts

7. Ensure nested groqResponse object is parsed correctly.

8. If route navigation state is missing after refresh, fetch diagnosis by Firestore document ID instead.


Ensure Firestore query returns array correctly and map over documents safely.

10. Add console logging for:
- API response
- Firestore response
- route state
- rendering data

11. Prevent undefined crashes using fallback UI.

12. Create proper diagnosis result cards:
- Plant identified
- Disease name
- Confidence score
- Symptoms
- Short-term treatment
- Long-term treatment
- Fertilizer recommendation
- Prevention tips

13. Do not redirect to blank page on success.